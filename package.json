{
  "name": "cli-team-bridge",
  "version": "0.1.0",
  "description": "ACP multi-agent coordinator — bridges Claude Code teams to external coding agents",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --watch run src/index.ts",
    "mcp": "bun run src/index.ts --mode mcp --config bridge.config.local.json",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "^0.14.1",
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "bun-types": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
